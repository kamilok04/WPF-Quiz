<UserControl x:Class="Rozwiazywarka.View.AnswerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Rozwiazywarka.View"    
             xmlns:viewmodel="clr-namespace:Rozwiazywarka.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        
        <viewmodel:AnswerConverter x:Key="AnswerConverter"/>
        <Style TargetType="ToggleButton" x:Key="UnselectedAnswer">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Background" Value="Red"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="Green"/>
                </Trigger>
                <Trigger Property="IsChecked" Value="False">
                    <Setter Property="Background" Value="Red"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
    </UserControl.Resources>
    <!--<UserControl.DataContext>
        <viewmodel:AnswerViewModel></viewmodel:AnswerViewModel>
    </UserControl.DataContext>-->
   
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"></ColumnDefinition>
            <ColumnDefinition Width="3*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
  
           
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"></RowDefinition>
                    <RowDefinition Height="1*"></RowDefinition>
                    <RowDefinition Height="10*"></RowDefinition>
                </Grid.RowDefinitions>
                <Button>Wyjście</Button>
                <Label Grid.Row="1">
                    Wybór pytania
                </Label>
                <UniformGrid Name="SelectionGrid" Grid.Row="2">
                    <ItemsControl ItemsSource="{Binding QuizStatus.Questions}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="4" Rows="4"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button>A</Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </UniformGrid>
            </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"></RowDefinition>
                <RowDefinition Height="10*"></RowDefinition>
            </Grid.RowDefinitions>
            <DockPanel>
                <Label>Upłynęło:</Label>
                <Label Content="{Binding TimeString, Mode=OneWay}"></Label>
            </DockPanel>
            <Grid VerticalAlignment="Stretch" Grid.Row="1">
                
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="4*"></ColumnDefinition>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="4*"></RowDefinition>
                    <RowDefinition Height="4*"></RowDefinition>
                   
                </Grid.RowDefinitions>
                <Label Grid.ColumnSpan="2" Content="{Binding QuizStatus.CurrentQuestion.Text}"></Label>
                <ItemsControl Name="AnswerList" ItemsSource="{Binding QuizStatus.CurrentAnswers}" Grid.Row="1">
                    
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="1"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="AliceBlue" BorderThickness="2" CornerRadius="5">
                                <ToggleButton Command="{Binding DataContext.SelectAnswer, RelativeSource={RelativeSource AncestorType=UserControl}}"  
                                       CommandParameter="{Binding ElementName=AnswerKey, Path=Content}" Checked="{Binding Answers}"
                                        Style="{StaticResource UnselectedAnswer}">  
                                  <DockPanel Name="AnswerChoice">
                               
                                        <Label Name="AnswerKey">  
                                          <Label.Content>  
                                              <MultiBinding Converter="{StaticResource AnswerConverter}">  
                                                  <Binding Path="."/>  
                                                  <Binding ElementName="AnswerList" Path="ItemsSource"></Binding>  
                                              </MultiBinding>  
                                          </Label.Content>  
                                      </Label>  
                                      <Label Content="{Binding Text}"></Label>  
                                  </DockPanel>  
                                </ToggleButton>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Button Grid.Column="1" Grid.Row="1"  Command="{Binding ConfirmAnswer}">
                    Dalej >
                </Button>
            </Grid>
        </Grid>
    </Grid>


</UserControl>
